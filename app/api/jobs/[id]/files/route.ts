import { requireUser } from "../../../../../lib/auth";
import { camelFile,supabaseAdmin } from "../../../../../lib/supabase";
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){if(!await requireUser(req))return Response.json({error:"กรุณาเข้าสู่ระบบ"},{status:401});const {id}=await params,{data}=await supabaseAdmin().from("delivery_files").select().eq("job_id",Number(id)).order("created_at",{ascending:false});return Response.json({files:(data??[]).map(camelFile)})}
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const u=await requireUser(req);if(!u)return Response.json({error:"กรุณาเข้าสู่ระบบ"},{status:401});const {id}=await params,jobId=Number(id),db=supabaseAdmin();const {data:job}=await db.from("jobs").select("id,state").eq("id",jobId).eq("state","accepted").maybeSingle();if(!job)return Response.json({error:"ต้องรับงานก่อนแนบไฟล์"},{status:409});const form=await req.formData(),file=form.get("file");if(!(file instanceof File)||!file.size)return Response.json({error:"ไม่พบไฟล์"},{status:400});if(file.size>50*1024*1024)return Response.json({error:"ไฟล์ต้องไม่เกิน 50 MB"},{status:413});const safe=file.name.replace(/[^a-zA-Z0-9._-]+/g,"-"),path=`jobs/${jobId}/${Date.now()}-${crypto.randomUUID()}-${safe}`;const up=await db.storage.from("delivery-files").upload(path,file,{contentType:file.type||"application/octet-stream"});if(up.error)throw up.error;const {data,error}=await db.from("delivery_files").insert({job_id:jobId,uploaded_by:u.id,storage_path:path,file_name:file.name,mime_type:file.type||"application/octet-stream",file_size:file.size}).select().single();return error?Response.json({error:error.message},{status:500}):Response.json({file:camelFile(data)},{status:201})}
